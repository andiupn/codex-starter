# Experiments

Folder ini dipakai untuk benchmark, eval, dan workflow test yang terpisah dari project aktif.

## Saran Struktur

- satu eksperimen satu folder
- simpan `README.md` dengan tujuan, command run, expected result, dan observed result
- gunakan `.env.example` bila eksperimen butuh environment variable
