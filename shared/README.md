# Shared

Folder ini untuk hal-hal reusable lintas project aktif.

Gunakan `shared/` untuk:

- common assets
- reusable config snippet
- helper kecil yang tidak layak menjadi project sendiri

Jika sesuatu punya lifecycle sendiri sebagai app, taruh di `active/`, bukan di sini.
